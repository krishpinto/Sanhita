# Railway's builder installs from this file. Kept in step with pyproject.toml
# by hand -- there are six lines and one deploy target, so a lockfile and a
# sync step would cost more than they save.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
sqlmodel>=0.0.22
httpx>=0.27
# Postgres driver, for Neon. psycopg 3, not psycopg2 -- hence the
# postgresql+psycopg:// prefix that app/config.py normalises URLs to.
psycopg[binary]>=3.2

# The AI second opinion talks to Gemini over plain REST with httpx above, so
# it needs nothing extra. Uncomment this only if you switch to ANTHROPIC_API_KEY
# -- app/ai/factory.py imports it lazily, so an unused line here is pure weight.
# anthropic>=0.40
